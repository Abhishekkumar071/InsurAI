spring:
  application:
    name: backend

  datasource:
    url: jdbc:mysql://localhost:3306/insurai_db?createDatabaseIfNotExist=true
    username: root
    password: test123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080