// UserDashboard was intentionally removed from routes and UI.
// This stub remains to avoid accidental import errors; the component returns null.
import React from 'react';

export default function UserDashboard() {
  return null;
}
